import os

EnsureSConsVersion(2, 1)

PREFIX = '/usr/local'
BINDIR = os.path.join(PREFIX, 'bin')

VariantDir('build', 'src')
program = SConscript('build/Sconscript')

Install(BINDIR, program)
Alias('install', BINDIR)
